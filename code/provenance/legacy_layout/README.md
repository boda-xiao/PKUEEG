# Historical layout tools

These unchanged builders/verifiers describe the old directory organization.
They are retained as provenance, not current release validation entry points.
Do not execute them on this reorganized release.

Their templates describe earlier behavior packaging and report paths, not the
current publication scope. Do not use them to reintroduce deferred behavioral
accuracy or technical-validation outputs. See code/config/release_scope.json
at the dataset root for the current scope.
